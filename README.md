## Datalevin native-image library example

This repo contains a minimal example of a native-image application
embedding datalevin as a library.

To test it out, run:

``` sh
./build.sh && ./example
```
